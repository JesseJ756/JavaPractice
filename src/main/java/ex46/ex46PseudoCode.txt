// METHOD(input file name): put input from file into ArrayList. RETURN ARRAYLIST
    // WHILE next line of file != EOF
        // SPLIT ' ' input line for String and put into ArrayLIst
        // Scan for NEXTLINE of file
    // RETURN ARRAYLIST

//METHOD(ARRAYLIST): make array list, iterate through input word ARRAYLIST and +1 everytime word is found
    //FOR word in lineArray
        //For identifiedWord in identifiedWordArray
            //IF word == identifiedWord
                // +1 for # in wordCount array
                // IsidentifiedWord = 1
        //IF isidentifiedword is 0
            //add word to idntifiedWordArray
            // +1 to # in wordCount array

// METHOD(count Array): bubble sort the count array so that its sorted highes to lowest
    // WHILE there is a swap
        // FOR each cell in count array from highest to lowest
            // IF array count[j] > array count[j - 1]
                // swap count[j] and count[j - 1]
                // swap wordArray[j] and wordArray[j - 1]
                // swapped = true

//METHOD to print ouput
    //FOR word in identifiedWordArray
        // PRINT word
        //FOR i < wordCountArray
            // print *
        // print \n

// WHILE loop, set counter to 0, iterate through file until EOF
// put name into array
// set number of names var to i
